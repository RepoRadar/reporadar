---
phase: 03-threshold-alerts
plan: 05
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - app/components/NotificationSignup.tsx
  - app/api/notifications/list/route.ts
  - tests/alerts-panel.spec.ts
autonomous: false
requirements: [ALRT-05]
must_haves:
  truths:
    - "A user can create an alert (term + kind + metric + threshold + window) from the panel"
    - "After creating, the user sees a 'check your email to confirm' acknowledgement (double opt-in)"
    - "The panel lists the user's active alerts and lets them remove/unsubscribe one"
    - "The panel reuses dashboard styling and does NOT touch any frozen UI contract"
  artifacts:
    - path: "app/components/NotificationSignup.tsx"
      provides: "Alerts panel: create + list + remove (extends the existing component, D-10)"
      contains: "metric"
    - path: "app/api/notifications/list/route.ts"
      provides: "GET subscriptions for an email (panel list source)"
      exports: ["GET"]
    - path: "tests/alerts-panel.spec.ts"
      provides: "browser QA: create an alert, confirm it lists (verify RESULTS not mechanics)"
      contains: "alert"
  key_links:
    - from: "app/components/NotificationSignup.tsx"
      to: "/api/notifications/subscribe"
      via: "fetch on create"
      pattern: "notifications/subscribe"
    - from: "app/components/NotificationSignup.tsx"
      to: "/api/notifications/list + /api/notifications/unsubscribe"
      via: "fetch for list + remove"
      pattern: "notifications/(list|unsubscribe)"
---

<objective>
Turn the existing `NotificationSignup` demo into a real Alerts panel (D-10): a form to create an alert (term + kind topic/query + metric + threshold + window_days), a list of the user's active alerts, and a remove/unsubscribe affordance — all calling the Plan 03 D1 routes, reusing dashboard styling and the green/blue/yellow/red gradient language, without touching any hackathon-frozen UI contract.

Purpose: ALRT-05 — the user-facing surface that makes alerts usable. Verification must confirm RESULTS (type a term → it actually lists after confirm), not just that a request fired (AGENTS.md verify-results rule).
Output: An extended Alerts panel, a `GET /api/notifications/list` route for the panel, and a Playwright spec proving create → list end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-threshold-alerts/03-CONTEXT.md

<read_first>
- `app/components/NotificationSignup.tsx` — the component to EXTEND (D-10). Note: `"use client"`, the existing `track("alert_signup", ...)` call (keep it — Phase 2), the existing on-brand styling tokens (`var(--primary)`, `var(--secondary)`, `var(--border)`, gradients), the `STORAGE_KEY` localStorage pattern for remembering the email. Reuse all of it; do NOT touch the frozen header/cards/sliders/radar.
- `app/api/notifications/subscribe/route.ts` (Plan 03) — the POST contract the create form calls: body `{ email, kind, term, metric, threshold, window_days }`, generic `{ ok: true, status: "pending_verification" }` response.
- `app/api/notifications/unsubscribe/route.ts` (Plan 03) — the GET `?token=` remove path (the list returns each sub's `unsub_token` so the panel can build a remove link/button).
- `app/lib/db.ts` (Plan 01) — `listSubscriptionsByEmail` (the list route calls it), `SubscriptionRow` shape.
- `AGENTS.md` "Current UI Contracts" + "Verification Expectations" — gradient color language for threshold UI; verify RESULTS not mechanics; UI freeze.
- `tests/notifications.spec.ts` + `tests/smoke.spec.ts` — existing Playwright style + selectors to follow for the new spec.
- `node_modules/next/dist/docs/` — READ the Next 16 route-handler doc before writing the list GET route.
</read_first>

<interfaces>
<!-- Plan 03 subscribe POST contract -->
```typescript
// POST /api/notifications/subscribe  body:
{ email: string; kind: "topic" | "query"; term: string; metric: "stars_pct"|"stars_abs"|"velocity"; threshold: number; window_days: number }
// -> 200 { ok: true, status: "pending_verification" }  (generic) | 400 invalid | 429 rate-limited
```
<!-- Plan 01 db.ts -->
```typescript
listSubscriptionsByEmail(db, email): Promise<SubscriptionRow[]>;  // id, term, kind, metric, threshold, window_days, verified_at, unsub_token, ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GET /api/notifications/list route (panel list source)</name>
  <files>app/api/notifications/list/route.ts</files>
  <action>
Create `app/api/notifications/list/route.ts` (`export const runtime = "nodejs"`, `GET`). Read `email` from `req.nextUrl.searchParams`, `normalizeEmail` it (reject invalid → 400). Get DB via `getCloudflareContext().env.DB`. Call `listSubscriptionsByEmail(db, email)` and return the alerts the panel needs — for each: `{ id, term, kind, metric, threshold, window_days, verified: <verified_at != null>, unsubToken: <unsub_token> }`. Do NOT return `verify_token` (don't expose the verify capability). Returning `unsub_token` to the email owner is acceptable — it is their own one-click remove capability — but apply the same per-IP rate-limit pattern as subscribe to avoid using this as an enumeration oracle (and keep the response generic/empty for an email with no subs rather than erroring). Read the Next 16 route-handler doc first.
  </action>
  <verify>
  <automated>cd /Users/cro/dev/reporadar && grep -q "export async function GET" app/api/notifications/list/route.ts && grep -q "listSubscriptionsByEmail" app/api/notifications/list/route.ts && ! grep -q "verify_token" app/api/notifications/list/route.ts && grep -q "getCloudflareContext" app/api/notifications/list/route.ts && echo OK</automated>
  </verify>
  <done>GET list route returns the caller's alerts by email (verified flag + unsubToken, never verify_token), rate-limited, `runtime="nodejs"`.</done>
</task>

<task type="auto">
  <name>Task 2: Extend NotificationSignup into the Alerts panel (create + list + remove)</name>
  <files>app/components/NotificationSignup.tsx</files>
  <action>
Extend the existing `"use client"` component (D-10) into an Alerts panel. Keep the existing email field + localStorage `STORAGE_KEY` remember + the `track("alert_signup", { sources: ... })` call (it stays — Phase 2 contract). Add a CREATE form with: a term text input, a `kind` toggle (topic | query), a `metric` select (stars_pct | stars_abs | velocity), a `threshold` number input, and a `window_days` number input (sensible defaults: kind=topic, metric=stars_pct, threshold=20, window_days=7). On submit, POST to `/api/notifications/subscribe` with `{ email, kind, term, metric, threshold, window_days }`. On the generic success, show a "Check your email to confirm this alert" acknowledgement (double opt-in — D-07). Replace the old "demo email queued" copy and the dummy `digest`/`sources` preview UI (it was for the v1 dummy email) with this real flow — but keep the component on-brand and additive.

Add a LIST section: on mount (and after a successful create) `fetch('/api/notifications/list?email=' + encodeURIComponent(email))` and render each alert as a row (term, metric, threshold, window, a "pending confirmation" vs "active" badge from the `verified` flag) with a Remove button that hits `/api/notifications/unsubscribe?token=<unsubToken>` and refreshes the list.

Styling: reuse the existing tokens + the green/blue/yellow/red gradient language for the threshold control where it reads naturally (AGENTS.md slider/score color contract) — e.g. the metric/threshold accents. Do NOT touch the frozen header, repo cards, sliders, or radar. Keep accessibility: labels on every input, `aria-live` on the status region (the component already has this — preserve it).
  </action>
  <verify>
  <automated>cd /Users/cro/dev/reporadar && grep -q "notifications/subscribe" app/components/NotificationSignup.tsx && grep -q "notifications/list" app/components/NotificationSignup.tsx && grep -q "notifications/unsubscribe" app/components/NotificationSignup.tsx && grep -q "stars_pct\|metric" app/components/NotificationSignup.tsx && grep -q "track(\"alert_signup\"" app/components/NotificationSignup.tsx && npx eslint app/components/NotificationSignup.tsx 2>&1 | grep -vE "set-state-in-effect|RepoRadarApp" | grep -cE "error" | grep -q "^0$" && echo OK</automated>
  </verify>
  <done>The panel creates an alert (POST subscribe with term/kind/metric/threshold/window), shows a confirm-your-email acknowledgement, lists the email's alerts with active/pending badges, and removes via the unsubscribe route; existing `track("alert_signup")` preserved; no new eslint errors; frozen UI untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify the Alerts panel results and frozen-UI integrity</name>
  <action>Pause for human verification of the rendered Alerts panel. No autonomous file changes — the reviewer confirms create-&gt;list results (verify RESULTS, not mechanics) and that the hackathon-frozen UI is intact.</action>
  <what-built>
The Alerts panel now creates a real alert (term + kind + metric + threshold + window), acknowledges double opt-in, lists the email's alerts with active/pending status, and removes via one-click unsubscribe — reusing dashboard styling and the gradient language, with the hackathon-frozen UI untouched. Backed by the Plan 03 D1 routes + the new list route.
  </what-built>
  <how-to-verify>
1. Apply local migrations + run dev: `wrangler d1 migrations apply reporadar --local`, then `npm run dev` (or the OpenNext preview if D1 binding is needed locally).
2. Open the dashboard. In the Alerts panel: TYPE a real term (e.g. "rust"), pick metric `stars_pct`, threshold `20`, window `7`, submit. CONFIRM the UI shows "check your email to confirm" (not the old demo-email copy) and that the alert appears in the list as PENDING (since verify hasn't been clicked). This is the verify-RESULTS rule — confirm the alert actually lists, not just that the POST returned 200.
3. Confirm the frozen UI is intact: header text present, repo cards/rank/star layout unchanged, sliders + radar unchanged.
4. Optionally hit the local verify route with the row's token and re-load → the alert flips to ACTIVE.
5. Click Remove on an alert → it disappears from the list.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. layout, a frozen-contract regression, list not reflecting the created alert).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Playwright spec — create an alert, confirm it lists</name>
  <files>tests/alerts-panel.spec.ts</files>
  <action>
Add `tests/alerts-panel.spec.ts` (follow the existing `tests/notifications.spec.ts` / `tests/smoke.spec.ts` style + config). The spec drives the LOCAL dev server: load the dashboard, locate the Alerts panel, TYPE a term + set metric/threshold/window, submit, and ASSERT the RESULT — the "check your email to confirm" acknowledgement appears AND the created alert shows in the list as pending (AGENTS.md: verify results, not mechanics — assert the rendered alert row, not just that the request fired). Add an assertion that the frozen header text ("AI Tinkerers Generative UI Hackathon") is still present on the page (guards the UI freeze). Keep it in the local suite (not the prod smoke suite) since it needs local D1; note the run command (`playwright test tests/alerts-panel.spec.ts` against local dev) in the SUMMARY.
  </action>
  <verify>
  <automated>cd /Users/cro/dev/reporadar && grep -q "confirm\|pending\|alert" tests/alerts-panel.spec.ts && grep -q "AI Tinkerers" tests/alerts-panel.spec.ts && npx playwright test tests/alerts-panel.spec.ts --list 2>&1 | grep -qiE "alerts-panel|alert" && echo OK</automated>
  </verify>
  <done>Spec types a term, submits, and asserts the alert lists (pending) + the confirm acknowledgement, plus that the frozen header is intact; it is discoverable by Playwright.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → list route | Caller-supplied email queries their own subscriptions |
| panel → subscribe/unsubscribe | Reuses the Plan 03 routes' boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-19 | Information Disclosure | GET /api/notifications/list | mitigate | Returns only the queried email's rows; never returns `verify_token`; rate-limited; generic empty response for an email with no subs (no enumeration oracle). |
| T-03-20 | Tampering | client-supplied metric/kind/threshold | mitigate | The subscribe route (Plan 03) re-validates via allow-list normalizers server-side — the panel's selects are convenience, not the trust boundary. |
| T-03-21 | (UI freeze) | frozen header/cards/sliders/radar | mitigate | Panel is additive; Playwright spec asserts the frozen header text persists; reviewer confirms cards/sliders/radar unchanged at the checkpoint. |
</threat_model>

<verification>
- `npx playwright test tests/alerts-panel.spec.ts` (against local dev + local D1) passes: alert created → lists as pending; confirm acknowledgement shown; frozen header present.
- `npm run build` clean; `npx eslint app/components/NotificationSignup.tsx app/api/notifications/list/route.ts` adds no NEW errors (2-baseline allowed).
- Human checkpoint: type a real term → the alert actually lists (verify RESULTS); frozen UI intact.
</verification>

<success_criteria>
- ALRT-05: an Alerts UI panel creates (term + metric + threshold), lists active alerts, and unsubscribes — reusing dashboard styling + gradient language, frozen contracts untouched.
- Create reflects the double-opt-in flow (pending until confirmed) and verification confirms the rendered result, not just the request.
</success_criteria>

<output>
After completion, create `.planning/phases/03-threshold-alerts/03-05-SUMMARY.md`
</output>
