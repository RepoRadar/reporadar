---
phase: 02-credibility-batch-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - app/content/changelog.ts
  - app/content/blog/index.ts
  - app/content/blog/why-we-built-reporadar.ts
  - app/content/blog/how-reporadar-scores-repos.ts
  - app/content/blog/_template-30-day-checkin.ts
  - app/(site)/layout.tsx
  - app/(site)/_components/Prose.tsx
  - app/(site)/changelog/page.tsx
  - app/(site)/blog/page.tsx
  - app/(site)/blog/[slug]/page.tsx
autonomous: true
requirements: [CRED-01, CRED-02]
must_haves:
  truths:
    - "A visitor can open /changelog and read a curated, on-brand changelog"
    - "A visitor can open /blog and see a list of 1-2 posts"
    - "A visitor can open /blog/<slug> and read a full markdown post"
    - "All three pages are statically rendered at build time (no runtime filesystem reads)"
  artifacts:
    - path: "app/content/changelog.ts"
      provides: "Curated changelog markdown bundled at build time"
      contains: "export const changelog"
    - path: "app/content/blog/index.ts"
      provides: "Ordered array of blog posts for the /blog list"
      contains: "export const posts"
    - path: "app/(site)/_components/Prose.tsx"
      provides: "On-brand react-markdown renderer using globals.css tokens"
      contains: "react-markdown"
    - path: "app/(site)/changelog/page.tsx"
      provides: "Static /changelog page"
      contains: "force-static"
    - path: "app/(site)/blog/page.tsx"
      provides: "Static /blog list page"
      contains: "force-static"
    - path: "app/(site)/blog/[slug]/page.tsx"
      provides: "Static /blog/[slug] post page"
      contains: "generateStaticParams"
  key_links:
    - from: "app/(site)/blog/page.tsx"
      to: "app/content/blog/index.ts"
      via: "import posts (build-time)"
      pattern: "import.*content/blog"
    - from: "app/(site)/blog/[slug]/page.tsx"
      to: "app/content/blog/index.ts"
      via: "generateStaticParams over post slugs"
      pattern: "generateStaticParams"
    - from: "app/(site)/changelog/page.tsx"
      to: "app/content/changelog.ts"
      via: "import changelog string"
      pattern: "import.*content/changelog"
---

<objective>
Ship the credibility content surfaces: a `(site)` route group with a shared on-brand article layout, a curated `/changelog`, and a `/blog` with a list and per-post pages. Content is bundled as TypeScript modules and rendered with react-markdown — all statically generated (CRED-01, CRED-02).

Purpose: RepoRadar must look like a real, maintained product with a public changelog and "how & why we built it" posts.
Output: `(site)` route group + Prose component + content modules + 3 static pages, plus 2 new deps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-credibility-batch-analytics/02-CONTEXT.md

<critical>
D-00 [BLOCKING]: Prod is Cloudflare Workers / OpenNext — NO runtime filesystem. Every new page MUST be statically rendered with content bundled at build time. NO `fs.readFileSync` / `fs.promises` at request time. Each page sets `export const dynamic = "force-static"` and MUST appear as `○ (Static)` in `npm run build` output. A page that reads files at runtime is WRONG and will 500 in prod.

AGENTS.md: This is Next.js 16. READ `node_modules/next/dist/docs/01-app/` for route groups, `generateStaticParams`, and `dynamic = "force-static"` BEFORE writing any route code. Do NOT touch the frozen header/cards/sliders/radar (this plan creates only new files under `app/(site)/` and `app/content/` plus `package.json` — it touches nothing frozen).
</critical>

<read_first>
- `node_modules/next/dist/docs/01-app/02-guides/static-exports.md` and the route-group / generateStaticParams docs under `node_modules/next/dist/docs/01-app/` (MANDATORY — Next 16 conventions differ from training data).
- `app/page.tsx` — existing async Server Component page pattern (`searchParams: Promise<{...}>`, `export const dynamic`, `export const revalidate`). Note this home page is `force-dynamic`; the new content pages are the opposite (`force-static`).
- `app/d/[slug]/page.tsx` — existing `[slug]` dynamic-segment page + `export const runtime`.
- `app/globals.css` (lines 3-42) — color/font tokens: `--bg #06080d`, `--surface #10141b`, `--fg #fafafa`, `--fg-muted #b3bbc8`, `--fg-dim #6b7384`, `--primary #22c55e` (links), `--secondary #3b82f6` (timestamps), `--font-geist-mono` (code). Use these tokens; introduce no new CSS system.
- `app/layout.tsx` — root layout already sets `bg-zinc-950 text-zinc-100` and font variables; the `(site)` layout nests inside it.
</read_first>

<interfaces>
Blog post module shape (D-02), define and reuse exactly:
```typescript
export type BlogPost = {
  slug: string;      // url-safe, e.g. "why-we-built-reporadar"
  title: string;
  date: string;      // ISO date, e.g. "2026-05-27"
  summary: string;   // 1-2 sentence list-card blurb
  body: string;      // markdown string
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deps + bundled content modules</name>
  <files>package.json, app/content/changelog.ts, app/content/blog/index.ts, app/content/blog/why-we-built-reporadar.ts, app/content/blog/how-reporadar-scores-repos.ts, app/content/blog/_template-30-day-checkin.ts</files>
  <action>
1. Add deps: run `npm install react-markdown remark-gfm` (D-01). Do NOT add `@next/mdx` or `gray-matter`.
2. Create `app/content/changelog.ts` exporting a curated changelog markdown string (D-02): `export const changelog = \`...\``. Seed it with real, honest entries reflecting actual project history — e.g. an entry for the hackathon build, the Phase 1 prerequisites (app-owned GitHub token + tag caching, sendEmail lib), and this credibility batch. Use GitHub-flavored markdown with dated `## YYYY-MM-DD` headings. Keep it factual; do not invent features that do not exist.
3. Create the `BlogPost` type and 2 posts (D-02):
   - `app/content/blog/why-we-built-reporadar.ts` — exports `const post: BlogPost` with slug `why-we-built-reporadar`, an honest "why we built RepoRadar" narrative (built for the AI Tinkerers Generative UI Hackathon; the problem: finding the most meaningful repo to build upon). Markdown body.
   - `app/content/blog/how-reporadar-scores-repos.ts` — exports `const post: BlogPost` with slug `how-reporadar-scores-repos`, explaining the 10-dimension scoring model. Markdown body.
4. Create `app/content/blog/_template-30-day-checkin.ts` — a COMMENTED-OUT "30-day check-in" recurring template module (per the brief's specific idea). It must NOT export an active post (so it does not appear in the list); leave it as a commented scaffold a human copies later.
5. Create `app/content/blog/index.ts` — `export const posts: BlogPost[] = [...]` importing the 2 real posts, ordered newest-first by `date`. Define/re-export the `BlogPost` type here too.
  </action>
  <verify>
    <automated>cd /Users/cro/dev/reporadar && node -e "require('fs').readFileSync('app/content/changelog.ts'); require('fs').readFileSync('app/content/blog/index.ts'); require('fs').readFileSync('app/content/blog/_template-30-day-checkin.ts')" && grep -q '"react-markdown"' package.json && grep -q '"remark-gfm"' package.json && grep -q 'export const posts' app/content/blog/index.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"react-markdown"` AND `"remark-gfm"`; does NOT add `@next/mdx` or `gray-matter`.
    - `app/content/changelog.ts` contains `export const changelog`.
    - `app/content/blog/index.ts` contains `export const posts` and references both post slugs.
    - `app/content/blog/_template-30-day-checkin.ts` exists and exports NO active post (commented template only).
    - No file in `app/content/` calls `fs.` (build-time bundled strings only).
  </acceptance_criteria>
  <done>Two deps installed; changelog + 2 blog posts + commented 30-day template + index all exist as bundled TS modules with no runtime fs.</done>
</task>

<task type="auto">
  <name>Task 2: (site) route group layout + Prose renderer</name>
  <files>app/(site)/layout.tsx, app/(site)/_components/Prose.tsx</files>
  <action>
1. Read the Next 16 route-group doc first. Create `app/(site)/layout.tsx` — a shared layout for the content pages (route group `(site)` shares a layout WITHOUT adding a URL segment, per D-03). Render a quiet on-brand wrapper: a max-width centered column, a small back-to-dashboard link (`<a href="/">← RepoRadar</a>` styled with `--primary`), `{children}`, using globals.css tokens (`background: var(--bg)`, `color: var(--fg)`). No header/card/slider markup — this is a content shell only. Avoid a white flash (dark background set on the wrapper).
2. Create `app/(site)/_components/Prose.tsx` — a Server Component that takes `{ markdown: string }`, renders it with `react-markdown` + `remark-gfm` (D-01, D-04), and styles the output with globals.css tokens: body text `--fg`/`--fg-muted`, links `--primary` (#22c55e), code/`pre` in `--font-geist-mono` on `--surface`, headings `--fg`, blockquote border `--secondary`. Use a `components={{...}}` map or a wrapping `className` with token-driven inline styles — do NOT introduce a new CSS framework. Keep it a Server Component (no `"use client"`) so the pages stay static.
  </action>
  <verify>
    <automated>cd /Users/cro/dev/reporadar && grep -q "react-markdown" app/\(site\)/_components/Prose.tsx && grep -q "remark-gfm" app/\(site\)/_components/Prose.tsx && test -f "app/(site)/layout.tsx" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `app/(site)/layout.tsx` exists and renders `{children}` with `var(--bg)`/`var(--fg)` tokens and a back-to-dashboard link.
    - `app/(site)/_components/Prose.tsx` imports `react-markdown` and `remark-gfm` and has NO `"use client"` directive.
    - Prose links use `var(--primary)`; code uses `var(--font-geist-mono)`.
  </acceptance_criteria>
  <done>Shared `(site)` layout + token-driven Prose Server Component exist; both dark, on-brand, server-rendered.</done>
</task>

<task type="auto">
  <name>Task 3: Static /changelog, /blog, /blog/[slug] pages</name>
  <files>app/(site)/changelog/page.tsx, app/(site)/blog/page.tsx, app/(site)/blog/[slug]/page.tsx</files>
  <action>
1. `app/(site)/changelog/page.tsx`: `export const dynamic = "force-static";` Server Component that imports `{ changelog }` from `@/app/content/changelog` and renders `<Prose markdown={changelog} />` inside a heading "Changelog". Export `metadata` (title "Changelog — RepoRadar"). NO runtime fs (D-00).
2. `app/(site)/blog/page.tsx`: `export const dynamic = "force-static";` imports `{ posts }` from `@/app/content/blog`, renders the list — each item links to `/blog/${post.slug}` with title (`--fg`), date (`--secondary`), and summary (`--fg-muted`). Export `metadata` (title "Blog — RepoRadar").
3. `app/(site)/blog/[slug]/page.tsx`: `export const dynamic = "force-static";` Implement `export async function generateStaticParams()` returning `posts.map(p => ({ slug: p.slug }))` (D-03) so every post is pre-rendered. The page reads `params` (Next 16: `params: Promise<{ slug: string }>` — await it), finds the matching post in `posts`, calls `notFound()` if missing, and renders `<Prose markdown={post.body} />` under the post title + date. Export `generateMetadata` using the post title/summary.
4. Read the Next 16 `generateStaticParams` + dynamic-segment doc before writing — confirm the params-as-Promise shape and that `dynamic = "force-static"` with `generateStaticParams` produces fully static output.
  </action>
  <verify>
    <automated>cd /Users/cro/dev/reporadar && npm run build 2>&1 | tee /tmp/p01-build.log | grep -E "○|●|ƒ" | grep -E "/changelog|/blog" ; grep -E "(○|●) +/changelog" /tmp/p01-build.log && grep -E "(○|●) +/blog" /tmp/p01-build.log && echo BUILD_STATIC_OK</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 3 page files contains `dynamic = "force-static"`.
    - `app/(site)/blog/[slug]/page.tsx` contains `generateStaticParams`.
    - None of the 3 pages reference `fs.` (D-00).
    - `npm run build` succeeds and the build output marks `/changelog` and `/blog` as Static (`○`) — NOT `ƒ (Dynamic)`. `/blog/[slug]` appears as prerendered (`●` SSG) for each slug.
    - `npx eslint "app/(site)/**/*.tsx" app/content/**/*.ts` adds no NEW errors.
  </acceptance_criteria>
  <done>`/changelog`, `/blog`, and `/blog/[slug]` build as static/prerendered pages, render bundled content via Prose, and the build output confirms `○`/`●` (no runtime fs, no dynamic rendering).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time → bundle | Content authored by the team, compiled into the bundle; no untrusted input |
| visitor → static page | Read-only static HTML; no user input accepted on these routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering/Information Disclosure | react-markdown rendering of content | mitigate | Content is team-authored (trusted). react-markdown does NOT render raw HTML by default (no `rehype-raw`); do not add `rehype-raw`, so embedded HTML/script in markdown is inert. |
| T-02-02 | Denial of Service | runtime filesystem read on Workers | mitigate | D-00: all content bundled at build time, `dynamic = "force-static"`; build output verified `○`/`●`. No request-time fs that could 500/exhaust the Worker. |
| T-02-03 | Information Disclosure | secrets in content modules | accept | Content modules are static prose with no env/secret access; reviewed at authoring time. |
</threat_model>

<verification>
- `npm run build` clean; `/changelog` and `/blog` shown as `○` (Static), `/blog/[slug]` as prerendered `●`.
- `npx eslint "app/(site)/**/*.tsx" app/content/**/*.ts` introduces no NEW errors.
- Dashboard (`/`) unchanged — this plan adds only new files; frozen header/cards/sliders untouched.
- Manual: `/changelog`, `/blog`, `/blog/why-we-built-reporadar` render dark, on-brand, with no white flash.
</verification>

<success_criteria>
A visitor can open `/changelog` and `/blog` (with the 2 posts), each on-brand and statically rendered; `/blog/[slug]` renders each post; build output confirms static rendering with no runtime filesystem dependency.
</success_criteria>

<output>
After completion, create `.planning/phases/02-credibility-batch-analytics/02-01-SUMMARY.md`
</output>
