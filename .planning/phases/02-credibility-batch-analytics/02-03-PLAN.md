---
phase: 02-credibility-batch-analytics
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/feedback/route.ts
  - app/components/FeedbackWidget.tsx
autonomous: true
requirements: [CRED-04]
must_haves:
  truths:
    - "A visitor can toggle the existing FeedbackWidget between Review and Suggest a feature mode"
    - "Submitting in feature mode posts type:'feature' and produces a feature-framed issue with a feature-request label"
    - "The default behavior (review feedback) is unchanged and backward compatible"
    - "An external trigger (hash/query) can open the widget pre-set to feature mode"
  artifacts:
    - path: "app/api/feedback/route.ts"
      provides: "type-aware feedback handler (feedback|feature) with feature-request label routing"
      contains: "feature-request"
    - path: "app/components/FeedbackWidget.tsx"
      provides: "single widget with a Review/Suggest-a-feature segmented toggle + external open hook"
      contains: "feature"
  key_links:
    - from: "app/components/FeedbackWidget.tsx"
      to: "/api/feedback"
      via: "fetch POST includes type field"
      pattern: "type:"
    - from: "app/api/feedback/route.ts"
      to: "GitHub issue labels"
      via: "feature label when type==='feature'"
      pattern: "feature-request"
---

<objective>
Extend the EXISTING FeedbackWidget + `/api/feedback` with a "suggest a feature" path via a `type` field — a single widget with two modes, NOT a duplicate widget (CRED-04). Add an external open hook so a footer link can open it pre-set to feature mode.

Purpose: let visitors suggest features through the surface they already know, routed to a feature-framed GitHub issue.
Output: type-aware feedback route + segmented-toggle widget with external open mechanism.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-credibility-batch-analytics/02-CONTEXT.md

<critical>
EXTEND, do NOT duplicate (D-08/D-09, CRED-04). There must remain exactly ONE feedback widget and ONE /api/feedback route. The change is backward compatible: `type` defaults to `"feedback"`; existing callers and behavior are unchanged.
FeedbackWidget lives in the FROZEN header (RepoRadarApp ~782-784). You may modify FeedbackWidget.tsx internals (it is the suggestion surface), but do NOT change its placement, its trigger button's role in the header layout, or any other frozen header/card/slider markup. This plan touches NO other component files.
</critical>

<read_first>
- `app/api/feedback/route.ts` — body type `FeedbackBody` (lines 7-12), `POST` flow (25-66), `verifyFeedback` Gemini `systemInstruction` (line 89-90), `buildFallbackIssue` labels (line 190), `normalizeLabels` which force-adds `["user-feedback","triage"]` (277-283), `createGitHubIssue` (148-173). Add `type` here.
- `app/components/FeedbackWidget.tsx` — `useState(open)` + outside-click/Escape effect (29-43), `submit()` builds the POST body (60-69), title/placeholder copy (115-146), trigger button + `aria-label` (87-101), status UX (180-207). Add the toggle + external open hook here.
</read_first>

<interfaces>
Extended request body (backward compatible):
```typescript
type FeedbackBody = {
  feedback?: unknown;
  contact?: unknown;
  pageUrl?: unknown;
  context?: unknown;
  type?: "feedback" | "feature"; // NEW — defaults to "feedback"
};
```
External open mechanism (choose ONE, document it for 02-04 footer link): a `window` custom event, e.g.
```typescript
// to open in feature mode from anywhere (e.g. footer link):
window.dispatchEvent(new CustomEvent("reporadar:open-feedback", { detail: { type: "feature" } }));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: type-aware /api/feedback (feedback|feature)</name>
  <files>app/api/feedback/route.ts</files>
  <behavior>
    - POST with no `type` → behaves exactly as today (label set `["user-feedback","triage"]`, review framing). Backward compatible.
    - POST with `type:"feature"` → resulting issue labels include `"feature-request"`; Gemini systemInstruction is tuned toward feature framing; fallback issue (no GOOGLE_API_KEY) also carries `feature-request`.
    - POST with an invalid `type` value → treated as `"feedback"` (default), never errors.
  </behavior>
  <action>
1. Add `type?: "feedback" | "feature"` to `FeedbackBody`. Normalize it: `const type = body.type === "feature" ? "feature" : "feedback";` (default/guard).
2. Thread `type` into `verifyFeedback(...)`. When `type === "feature"`, use a feature-framed `systemInstruction` (e.g. "You turn RepoRadar user feature suggestions into well-scoped GitHub feature requests. Capture the user's desired outcome and motivation, separate the request from assumptions, never invent scope, output only JSON.") and adjust the JSON prompt's title prefix toward `Feature:` instead of `Feedback:`. When `type === "feedback"`, keep the current instruction verbatim.
3. Label routing: when `type === "feature"`, ensure `"feature-request"` is in the labels. The simplest correct approach: pass a base-labels argument into `normalizeLabels` / the fallback so feature mode seeds `["user-feedback","triage","feature-request"]` and review mode keeps `["user-feedback","triage"]`. Update `buildFallbackIssue` to honor `type` the same way.
4. Keep everything else (validation, createGitHubIssue, response shape) unchanged. Do NOT break the existing review path.
  </action>
  <verify>
    <automated>cd /Users/cro/dev/reporadar && grep -q 'type' app/api/feedback/route.ts && grep -q 'feature-request' app/api/feedback/route.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "feedback/route" ; echo "(no tsc errors above for feedback route = pass)"</automated>
  </verify>
  <acceptance_criteria>
    - `app/api/feedback/route.ts` contains `feature-request` and a `type` normalization defaulting to `"feedback"`.
    - The review path (no `type`) still produces `["user-feedback","triage"]` labels and the original review systemInstruction.
    - Feature path adds `feature-request` in BOTH the LLM and fallback issue paths.
    - Still exactly ONE POST handler; response shape unchanged.
  </acceptance_criteria>
  <done>/api/feedback accepts an optional type field; feature mode produces feature-framed, feature-request-labeled issues; review mode is byte-for-byte behavior-compatible.</done>
</task>

<task type="auto">
  <name>Task 2: FeedbackWidget Review/Suggest toggle + external open hook</name>
  <files>app/components/FeedbackWidget.tsx</files>
  <action>
1. Add a `type` state: `const [mode, setMode] = useState<"feedback"|"feature">("feedback");`.
2. Add a small segmented toggle inside the open panel (two buttons: "Review" / "Suggest a feature") styled with globals.css tokens (active = `--primary` border/glow like the existing trigger button). Selecting toggles `mode`.
3. Swap copy by mode: title ("Send review" ↔ "Suggest a feature"), the helper sentence, and the textarea placeholder ("What should be fixed, improved, or preserved?" ↔ "What feature would make RepoRadar more useful for you?"). Update the submit button label similarly.
4. Include `type: mode` in the POST body in `submit()` (the only body change).
5. Add the external open hook (D-09): in a `useEffect`, listen for `window` event `"reporadar:open-feedback"`; on receipt, `setOpen(true)` and `setMode(detail?.type === "feature" ? "feature" : "feedback")`. Clean up the listener on unmount. This is what the footer "Suggest a feature" link (02-04) will dispatch. Document the event name in a comment.
6. Keep the trigger button, placement, outside-click/Escape behavior, and status UX unchanged. Do NOT alter the header layout around the widget.
  </action>
  <verify>
    <automated>cd /Users/cro/dev/reporadar && grep -q 'reporadar:open-feedback' app/components/FeedbackWidget.tsx && grep -q 'feature' app/components/FeedbackWidget.tsx && grep -q 'type:' app/components/FeedbackWidget.tsx && npm run build 2>&1 | tail -5 && npx eslint app/components/FeedbackWidget.tsx app/api/feedback/route.ts 2>&1 | tail -20 ; echo DONE</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/FeedbackWidget.tsx` contains a Review/Suggest segmented toggle, sends `type: mode` in the POST body, and listens for `"reporadar:open-feedback"`.
    - Still ONE widget (no second/duplicate component file created).
    - `npm run build` succeeds; `npx eslint` on the two changed files adds no NEW errors (the 2 baseline RepoRadarApp set-state-in-effect warnings are NOT in these files).
    - The widget's header placement/trigger are unchanged (frozen UI intact).
  </acceptance_criteria>
  <done>The single FeedbackWidget toggles between Review and Suggest-a-feature, sends the correct type, and can be opened in feature mode by an external event — no duplicate widget, frozen header intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor (untrusted) → POST /api/feedback | feedback text + new `type` field are attacker-controllable |
| /api/feedback → GitHub issue (Octokit) | user input flows into an issue title/body/labels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering (label injection) | `type` → issue labels | mitigate | Normalize `type` to a strict enum (`"feature"` else `"feedback"`); labels are seeded from a fixed allowlist and `normalizeLabels` already strips to `[a-z0-9-]`. Attacker cannot inject arbitrary labels via `type`. |
| T-02-09 | Spam/DoS via issue creation | /api/feedback | accept | Existing route had no limiter; out of scope for this extension (no new exposure — same endpoint, one extra enum field). Issue creation still gated by `GITHUB_TOKEN` presence. Note for a future hardening pass. |
| T-02-10 | Tampering (existing field injection) | feedback/contact text in issue body | accept | Pre-existing behavior, unchanged by this plan; GitHub renders issue bodies as markdown server-side. No new surface introduced. |
</threat_model>

<verification>
- `npm run build` clean; `npx eslint app/components/FeedbackWidget.tsx app/api/feedback/route.ts` adds no NEW errors.
- Manual (RESULTS not mechanics): open widget → toggle to "Suggest a feature" → copy/placeholder change → submit → response issue carries `feature-request` (and feature-framed title); toggle back to Review → submit → original review behavior. Dispatch `reporadar:open-feedback` with `type:"feature"` from console → widget opens in feature mode.
- Exactly one feedback widget remains; frozen header untouched.
</verification>

<success_criteria>
A visitor can suggest a feature through the existing FeedbackWidget's "suggest a feature" path (not a duplicate widget), routed to a feature-request issue, with the review path unchanged and an external open hook available to the footer.
</success_criteria>

<output>
After completion, create `.planning/phases/02-credibility-batch-analytics/02-03-SUMMARY.md`
</output>
