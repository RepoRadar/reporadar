---
phase: 02-credibility-batch-analytics
plan: 04
type: execute
wave: 2
depends_on: [01, 03]
files_modified:
  - app/components/Footer.tsx
  - app/lib/links.ts
  - app/components/RepoRadarApp.tsx
autonomous: true
requirements: [CRED-05]
must_haves:
  truths:
    - "A visitor sees an on-brand footer below the dashboard grid"
    - "The footer links to Changelog, Blog, Contact, Suggest a feature, Buy us a coffee, GitHub, and the frozen hackathon link"
    - "Buy us a coffee opens DONATION_URL in a new tab (no payment integration)"
    - "Suggest a feature opens the FeedbackWidget in feature mode"
    - "The footer never overlaps or alters the frozen header/cards/sliders"
  artifacts:
    - path: "app/components/Footer.tsx"
      provides: "On-brand product footer with all required links"
      contains: "Buy us a coffee"
    - path: "app/lib/links.ts"
      provides: "DONATION_URL constant + link targets"
      contains: "DONATION_URL"
    - path: "app/components/RepoRadarApp.tsx"
      provides: "Footer mounted after the grid (~line 1068)"
      contains: "<Footer"
  key_links:
    - from: "app/components/RepoRadarApp.tsx"
      to: "app/components/Footer.tsx"
      via: "render <Footer/> after </main>"
      pattern: "<Footer"
    - from: "app/components/Footer.tsx"
      to: "FeedbackWidget feature mode"
      via: "dispatch reporadar:open-feedback {type:'feature'}"
      pattern: "reporadar:open-feedback"
    - from: "app/components/Footer.tsx"
      to: "DONATION_URL"
      via: "outbound anchor target=_blank"
      pattern: "DONATION_URL"
---

<objective>
Add an on-brand product footer beneath the dashboard with links to the credibility surfaces, the donation outbound link, GitHub, and the frozen hackathon link (CRED-05). Mount it AFTER the grid so it never touches the frozen UI.

Purpose: tie the new surfaces together and surface the "Buy us a coffee" donation link without payment integration.
Output: Footer component + links module + mount in RepoRadarApp + DONATION_URL handoff note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-credibility-batch-analytics/02-CONTEXT.md

<critical>
FROZEN UI: The footer mounts AFTER the grid (RepoRadarApp grid ends ~1068-1082, the `</main>` close), BELOW the dashboard — it MUST NOT overlap or alter the header (697-798), cards, sliders, or radar (AGENTS.md UI contracts + D-13). The ONLY RepoRadarApp edit is inserting `<Footer />` after `</main>` (and its import). Do not reorder, restyle, or touch anything inside the header or grid.
This plan DEPENDS ON 02-03 (the `reporadar:open-feedback` window event must exist) and 02-01 (the `/changelog`, `/blog`, `/contact` routes must exist). Sequenced after 02-03 to avoid editing the same files concurrently.
Keep the existing frozen hackathon link text exactly: "AI Tinkerers Generative UI Hackathon" → `https://sf.aitinkerers.org/hackathons/h_FZX7ihFWcHA/handbook`.
</critical>

<read_first>
- `app/components/RepoRadarApp.tsx` — the grid `</section></main>` close (~1067-1068) and `</div>` before the CopilotPopup (~1082); the FROZEN hackathon link (738-749) for the href/label to reuse; the FeedbackWidget mount (782-784). The Footer goes after `</main>`, before `<CopilotPopup>`.
- `app/components/FeedbackWidget.tsx` (after 02-03) — it now listens for `window` event `"reporadar:open-feedback"` with `detail.type`. The footer "Suggest a feature" link dispatches this.
- `app/globals.css` (3-42) — tokens for an on-brand quiet footer: `--bg`/`--surface`, `--fg-muted`/`--fg-dim` text, `--border` separators, `--primary` link hover. Footer must read as a real product footer, not a debug strip (specific idea in CONTEXT).
- `app/(site)/...` routes from 02-01: `/changelog`, `/blog`, `/contact` — the internal link targets.
</read_first>

<interfaces>
```typescript
// app/lib/links.ts
export const DONATION_URL =
  process.env.NEXT_PUBLIC_DONATION_URL || "https://ko-fi.com/reporadar"; // D-10 placeholder; real handle = human handoff
export const GITHUB_URL = "https://github.com/RepoRadar/reporadar";
export const HACKATHON_URL = "https://sf.aitinkerers.org/hackathons/h_FZX7ihFWcHA/handbook";
```
Footer "Suggest a feature" handler:
```typescript
onClick={() => window.dispatchEvent(new CustomEvent("reporadar:open-feedback", { detail: { type: "feature" } }))}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: links module + Footer component</name>
  <files>app/lib/links.ts, app/components/Footer.tsx</files>
  <action>
1. Create `app/lib/links.ts` exporting `DONATION_URL` (D-10: `process.env.NEXT_PUBLIC_DONATION_URL || "https://ko-fi.com/reporadar"`), `GITHUB_URL`, and `HACKATHON_URL` (reuse the exact frozen hackathon href).
2. Create `app/components/Footer.tsx` (`"use client"` — it needs the dispatch onClick) — a quiet, on-brand footer using globals.css tokens. Sections/links:
   - Internal: Changelog (`/changelog`), Blog (`/blog`), Contact (`/contact`) — plain `<a>` or `next/link`.
   - "Suggest a feature" — a button/link that dispatches `reporadar:open-feedback` with `{ type: "feature" }` (D-09).
   - "☕ Buy us a coffee" — `<a href={DONATION_URL} target="_blank" rel="noopener noreferrer">` with an accessible label (D-10). No payment integration.
   - GitHub — `<a href={GITHUB_URL} target="_blank" rel="noopener noreferrer">` with the GitHub mark + accessible label (do NOT use a fake icon for unrelated tech — GitHub mark is the real icon and is allowed).
   - The frozen hackathon link — exact text "AI Tinkerers Generative UI Hackathon" → `HACKATHON_URL`, new tab.
   - A small copyright/wordmark line ("RepoRadar" — white "Repo" + green "Radar" text per AGENTS.md wordmark rule) is fine but keep it quiet.
   Style: `border-top: 1px solid var(--border)`, muted text (`--fg-dim`/`--fg-muted`), links hover to `--primary`. Below-the-fold, not a debug strip. External links all `rel="noopener noreferrer"`.
3. NOTE: `NEXT_PUBLIC_DONATION_URL` is documented in `.dev.vars.example` by plan 02-05 (consolidated env doc) — do NOT edit `.dev.vars.example` here.
  </action>
  <verify>
    <automated>cd /Users/cro/dev/reporadar && grep -q "DONATION_URL" app/lib/links.ts && grep -q "reporadar:open-feedback" app/components/Footer.tsx && grep -q "Buy us a coffee" app/components/Footer.tsx && grep -q "AI Tinkerers Generative UI Hackathon" app/components/Footer.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `app/lib/links.ts` contains `DONATION_URL`, `GITHUB_URL`, `HACKATHON_URL`.
    - `app/components/Footer.tsx` contains links to `/changelog`, `/blog`, `/contact`, a `reporadar:open-feedback` dispatch, `DONATION_URL` with `target="_blank" rel="noopener noreferrer"`, the GitHub link, and the exact frozen hackathon text.
    - All external links use `rel="noopener noreferrer"`.
  </acceptance_criteria>
  <done>Footer component + links module exist with all required links, donation as a safe outbound new-tab link, and the frozen hackathon link reused verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Mount Footer after the grid in RepoRadarApp</name>
  <files>app/components/RepoRadarApp.tsx</files>
  <action>
1. Import `Footer` at the top of `RepoRadarApp.tsx` (alongside the other component imports near line 22-23).
2. Insert `<Footer />` AFTER the grid's `</main>` (~line 1068) and BEFORE `<CopilotPopup ... />` (~line 1070), inside the outer wrapper `<div>`. This places it below the dashboard, never overlapping (D-13).
3. Make NO other edits to RepoRadarApp.tsx. Do not touch the header (697-798), the grid/cards, the sliders, the radar, or the deploy dock. This is a 2-line change (1 import + 1 mount).
  </action>
  <verify>
    <automated>cd /Users/cro/dev/reporadar && grep -q "import { Footer }" app/components/RepoRadarApp.tsx && grep -q "<Footer" app/components/RepoRadarApp.tsx && npm run build 2>&1 | tail -5 && npx eslint app/components/RepoRadarApp.tsx app/components/Footer.tsx app/lib/links.ts 2>&1 | tail -25 ; echo DONE</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/RepoRadarApp.tsx` imports `Footer` and renders `<Footer` exactly once, positioned after `</main>` and before `<CopilotPopup`.
    - The diff to RepoRadarApp.tsx is ONLY the import line + the `<Footer />` mount (header/cards/sliders/radar bytes unchanged).
    - `npm run build` succeeds.
    - `npx eslint` on changed files adds no NEW errors beyond the 2 allowed baseline set-state-in-effect warnings already present in RepoRadarApp.tsx.
  </acceptance_criteria>
  <done>The Footer renders below the dashboard grid with no change to the frozen header/cards/sliders; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → external sites (donation, GitHub, hackathon) | outbound links open third-party origins in a new tab |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Tampering (reverse tabnabbing) | outbound `target="_blank"` links | mitigate | All external anchors set `rel="noopener noreferrer"` so the opened page cannot access `window.opener` or referrer. |
| T-02-12 | Spoofing (donation URL) | DONATION_URL | accept | Default is a hardcoded placeholder; the real handle is an owner-supplied env value (`NEXT_PUBLIC_DONATION_URL`) set at deploy. No user input controls it. |
| T-02-13 | UI Redress (footer overlapping frozen UI) | RepoRadarApp mount | mitigate | Footer mounts strictly after `</main>` below the grid; no absolute/fixed positioning over the dashboard; verified diff limited to import + mount. |
</threat_model>

<verification>
- `npm run build` clean; `npx eslint` on changed files adds no NEW errors (2 baseline warnings allowed).
- Manual: footer renders below the grid, on-brand and quiet; Changelog/Blog/Contact navigate correctly; "Buy us a coffee" opens DONATION_URL in a new tab; "Suggest a feature" opens the FeedbackWidget in feature mode; GitHub + hackathon links open in new tabs. Header/cards/sliders/radar visually unchanged.
</verification>

<success_criteria>
The dashboard has an on-brand footer linking the credibility surfaces plus a "Buy us a coffee" outbound donation link and a "Suggest a feature" hook into the existing widget — with the frozen UI fully intact.
</success_criteria>

<output>
After completion, create `.planning/phases/02-credibility-batch-analytics/02-04-SUMMARY.md`
</output>
