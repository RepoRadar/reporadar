---
phase: 04-repo-intelligence
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - app/chat/[owner]/[repo]/ChatClient.tsx
  - app/chat/[owner]/[repo]/page.tsx
autonomous: true
requirements: [INTL-01, INTL-04]
must_haves:
  truths:
    - "The left pane shows the four suggested-prompt chips when the thread is empty"
    - "Chips 1, 3, and 4 send immediately; chip 2 opens the describe-your-build gate and sends nothing until the user submits a description"
    - "Sending a message streams the assistant reply token by token, rendered as markdown with links opening in a new tab"
    - "A typing indicator shows while streaming, the send button becomes a stop button, and the composer is disabled"
    - "Messages live only in React state (no persistence); the not-saved note is visible"
    - "When the API key is absent the composer shows 'Chat is not available right now.' and the chips still display"
    - "Rate-limited responses show the friendly banner, never a 429 code"
  artifacts:
    - path: "app/chat/[owner]/[repo]/ChatClient.tsx"
      provides: "Client streaming chat UI: messages state, fetch-reader stream, react-markdown, 4 chips, chip-2 gate, composer, states"
      contains: "use client"
      min_lines: 200
  key_links:
    - from: "app/chat/[owner]/[repo]/ChatClient.tsx"
      to: "/api/repo-chat"
      via: "fetch POST + ReadableStream reader"
      pattern: "/api/repo-chat"
    - from: "app/chat/[owner]/[repo]/page.tsx"
      to: "ChatClient"
      via: "mount with fullName, repoName, apiKeyPresent props"
      pattern: "<ChatClient"
---

<objective>
Build `app/chat/[owner]/[repo]/ChatClient.tsx` ("use client") and mount it into the left pane of `page.tsx` (replacing the 04-03 placeholder). It holds the message array in ephemeral React state, streams from `/api/repo-chat` via a `ReadableStream` reader, renders assistant turns with `react-markdown` (links new-tab), shows the four suggested-prompt chips, implements the chip-2 describe-your-build gate (does not answer until the user describes their project), and renders every state from 04-UI-SPEC §States (streaming + stop, unavailable, rate-limited, error). No persistence; the not-saved note stays visible.

Purpose: This completes the interactive chat. It is the last build piece before QA. It depends on the endpoint (04-02) and the page shell + RepoPane (04-03).
Output: `ChatClient.tsx`, plus the one-line mount edit in `page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-repo-intelligence/04-UI-SPEC.md
@.planning/phases/04-repo-intelligence/04-PATTERNS.md
@.planning/phases/04-repo-intelligence/04-PRD.md
@.planning/phases/04-repo-intelligence/04-02-SUMMARY.md
@.planning/phases/04-repo-intelligence/04-03-SUMMARY.md
@app/(site)/_components/ContactForm.tsx
@app/(site)/_components/Prose.tsx

<interfaces>
<!-- From 04-02: endpoint contract. From 04-03: page mount point. Use directly. -->
POST /api/repo-chat
  body: { fullName: string, messages: { role: "user"|"assistant", content: string }[] }  // send last ~10 turns
  success: 200, body is a streamed text/plain assistant answer (read with res.body.getReader())
  errors (JSON, returned BEFORE any stream):
    503 { ok:false, error:"Chat is not available right now." }   // no GOOGLE_API_KEY
    429 { ok:false, error:"You're sending messages quickly. Give it a few seconds." }
    400 { ok:false, error: "..." }   // bad input
    502 { ok:false, error:"Could not load this repo." }

react-markdown client usage (04-RESEARCH Pitfall 6): in a "use client" component use the default `Markdown` (sync) export + remarkGfm, NOT MarkdownAsync. Mirror Prose.tsx Components but compact (h1-h3 at text-base font-semibold, p text-sm). Links `target="_blank" rel="noopener noreferrer"`. NO rehype-raw.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ChatClient.tsx (state machine, streaming reader, markdown)</name>
  <read_first>
    - app/(site)/_components/ContactForm.tsx ("use client" + useState state machine, fetch pattern, 429 handling, disabled-while-busy, role="status" aria-live)
    - app/(site)/_components/Prose.tsx (react-markdown Components map; copy + compact per UI-SPEC)
    - .planning/phases/04-repo-intelligence/04-02-SUMMARY.md (endpoint request/response + error shapes)
    - .planning/phases/04-repo-intelligence/04-RESEARCH.md ("Client-side stream reader" code, Pitfall 6 sync Markdown)
    - .planning/phases/04-repo-intelligence/04-UI-SPEC.md ("Left pane: conversation" message list / bubbles / typing indicator, "Composer", "Suggested prompt chips" + the chip-2 gate exact spec, "States", "Copywriting contract")
  </read_first>
  <action>
    Create `app/chat/[owner]/[repo]/ChatClient.tsx`. First line `"use client";`. Imports: `import { useRef, useState } from "react";` `import Markdown from "react-markdown";` `import remarkGfm from "remark-gfm";` `import type { Components } from "react-markdown";`.

    Props: `{ fullName: string; repoName: string; apiKeyPresent: boolean }`.

    State: `messages: { role: "user"|"assistant"; content: string }[]`, `status: "idle"|"streaming"|"error"|"rate-limited"`, `input: string`, `gateOpen: boolean`, `gateText: string`, an `AbortController` ref for the stop button.

    Markdown: a `Components` map mirroring Prose.tsx but compact per UI-SPEC (h1-h3 `text-base font-semibold var(--fg)`, p `text-sm leading-relaxed`, a `var(--secondary)` underline-on-hover `target="_blank" rel="noopener noreferrer"`, inline code `var(--surface-3)`/`var(--primary)`, pre block `var(--surface-3)`). Use the sync `<Markdown>` (Pitfall 6). NO rehype-raw.

    `sendMessage(text)`:
    - Append a user `{role:"user",content:text}` and an empty assistant `{role:"assistant",content:""}`; set status "streaming"; create a fresh AbortController.
    - `fetch("/api/repo-chat", { method:"POST", headers:{"content-type":"application/json"}, body: JSON.stringify({ fullName, messages: [...messages, {role:"user",content:text}].slice(-10) }), signal })`.
    - If `!res.ok`: parse JSON; if `res.status === 429` set status "rate-limited" and show the banner copy "You're sending messages quickly. Give it a few seconds." (auto-dismiss after 5s); otherwise replace the empty assistant bubble with `body.error ?? "Something went wrong. Try again."` and set status "error". Return.
    - Else read `res.body.getReader()` + `TextDecoder`, loop, accumulate, and update the last assistant message on each chunk (the 04-RESEARCH "Client-side stream reader" pattern). On AbortError, keep what streamed so far. `finally` set status "idle".

    Suggested chips (shown only when `messages.length === 0`), copy VERBATIM from UI-SPEC: chip 1 "Why did you score it this way?", chip 3 "Tell me what's so special about this repo.", chip 4 "This sounds like hype. Explain what I'm missing." send immediately via `sendMessage(chipText)`. Chip 2 "I'm building something, does this fit?" does NOT send: clicking toggles `gateOpen`, sets `aria-expanded`, and reveals an inline form (label "What are you building?", a 3-row textarea `aria-label="Describe what you are building"` with placeholder "Describe your project briefly, what it does, how it works, what it needs to connect to.", and a "Check the fit" submit). On submit with non-empty text, collapse the gate and `sendMessage("I'm building something, does this fit?\n" + gateText)`. On empty submit, flash the textarea border `var(--danger)` and send nothing. Chips style per UI-SPEC (border var(--border), bg var(--surface-3), font-mono, min-height 44px, hover green).

    Composer (pinned bottom): textarea `Ask anything about ${repoName}…` placeholder, 2000-char cap (show count within 200 of cap; disable send over cap), Enter sends / Shift+Enter newline. Send button (green) `aria-label="Send message"`; while streaming it is replaced by a Stop button (red, "Stop", `aria-label="Stop generating"`) that calls `abortController.abort()`. Composer disabled while streaming.

    Typing indicator: when streaming and the last assistant message is empty, render an assistant bubble with three `.rr-think-dot` dots, wrapped `aria-live="polite"` `aria-label="RepoRadar is thinking"`.

    Unavailable state: if `!apiKeyPresent`, render the chips (read-only guide) and, in place of the composer, the exact copy "Chat is not available right now." in a bordered `var(--surface-2)` box. No textarea, no send.

    Rate-limit banner: a dismissable banner above the composer (`var(--accent)` border/text, `× ` dismiss `aria-label="Dismiss rate limit notice"`), shown when status === "rate-limited".

    Message list: `tabIndex={0}` `aria-label="Conversation"` `aria-live="polite"`; user bubble right-aligned (var(--surface-3)), assistant bubble left-aligned (var(--surface-2)) rendered through `<Markdown>`. Bubble radii/padding per UI-SPEC.

    Privacy (INTL-04): messages live only in this component's React state. Do NOT write to localStorage, sessionStorage, cookies, IndexedDB, or any fetch other than `/api/repo-chat`. No analytics call carrying message text.

    Only UI-SPEC tokens. All copy VERBATIM from the Copywriting contract. Zero em dashes, sentence case.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c '—' app/chat/[owner]/[repo]/ChatClient.tsx && grep -cE 'localStorage|sessionStorage|document\.cookie|indexedDB' app/chat/[owner]/[repo]/ChatClient.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `app/chat/[owner]/[repo]/ChatClient.tsx` first line is `"use client";` and it imports the sync `Markdown` default export (`grep -q 'import Markdown from "react-markdown"' ChatClient.tsx`); no `MarkdownAsync`.
    - `grep -q '/api/repo-chat' ChatClient.tsx` and `grep -q 'getReader()' ChatClient.tsx` (streaming reader).
    - All four chip strings present verbatim: `grep -q 'Why did you score it this way?'`, `grep -q "I'm building something, does this fit?"`, `grep -q "Tell me what's so special about this repo."`, `grep -q 'This sounds like hype. Explain what I'm missing.'`.
    - Chip-2 gate: `grep -q 'What are you building?' ChatClient.tsx` and `grep -q 'Check the fit' ChatClient.tsx` and `grep -q 'aria-expanded' ChatClient.tsx`.
    - `grep -q 'Chat is not available right now.' ChatClient.tsx` (unavailable copy) and `grep -q "You're sending messages quickly" ChatClient.tsx` (rate-limit copy).
    - `grep -q 'rehype-raw' ChatClient.tsx` returns nothing.
    - `grep -cE 'localStorage|sessionStorage|document\.cookie|indexedDB' ChatClient.tsx` returns 0 (no client persistence, INTL-04).
    - `grep -c '—' ChatClient.tsx` returns 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Mount ChatClient in the page left pane</name>
  <read_first>
    - app/chat/[owner]/[repo]/page.tsx (04-03, the `{/* ChatClient mounts here in 04-04 */}` placeholder and the props it already prepares)
    - app/chat/[owner]/[repo]/ChatClient.tsx (Task 1, its prop signature)
    - .planning/phases/04-repo-intelligence/04-03-SUMMARY.md (the exact props the page exposes)
  </read_first>
  <action>
    Edit `app/chat/[owner]/[repo]/page.tsx`: import `ChatClient` and replace the `{/* ChatClient mounts here in 04-04 */}` placeholder with `<ChatClient fullName={fullName} repoName={repo} apiKeyPresent={Boolean(process.env.GOOGLE_API_KEY)} />`. The left-pane chat header (repo name, trust line, not-saved note) stays above it (from 04-03). Do not change the right pane, header, or not-found state. Keep page.tsx a server component (ChatClient is the only "use client" island).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && grep -c '—' app/chat/[owner]/[repo]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '<ChatClient' app/chat/[owner]/[repo]/page.tsx` and `grep -q 'apiKeyPresent' page.tsx`.
    - `grep -q 'ChatClient mounts here' app/chat/[owner]/[repo]/page.tsx` returns nothing (placeholder replaced).
    - page.tsx has no `"use client"` directive (still a server component).
    - `npm run build` succeeds.
    - `grep -c '—' app/chat/[owner]/[repo]/page.tsx` returns 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/repo-chat | user-typed message content leaves the client |
| streamed assistant text → DOM | rendered as markdown in the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Information Disclosure | proprietary "what I'm building" input retained client-side (INTL-04) | mitigate | messages live only in React state; no localStorage/sessionStorage/cookie/IndexedDB writes; gone on tab close; not-saved note visible |
| T-04-15 | Tampering | XSS via streamed assistant markdown | mitigate | sync react-markdown, NO rehype-raw; links forced `target="_blank" rel="noopener noreferrer"` |
| T-04-16 | Denial of Service | runaway streaming with no stop | mitigate | AbortController-backed stop button cancels the fetch; server also caps tool rounds + rate-limits |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run build` clean.
- `grep -c '—' ChatClient.tsx page.tsx` returns 0.
- No client-side persistence APIs referenced in ChatClient.tsx.
- All chip, gate, unavailable, and rate-limit copy match the UI-SPEC Copywriting contract.
</verification>

<success_criteria>
The left pane chats: four chips with chip-2 gating on a build description, token-by-token streaming markdown answers, stop control, graceful unavailable + rate-limited states, and zero persistence with the not-saved note visible. Combined with 04-02 and 04-03 this delivers INTL-01 (the full talk-to-a-repo experience) and the client half of INTL-04 (no retention).
</success_criteria>

<output>
After completion, create `.planning/phases/04-repo-intelligence/04-04-SUMMARY.md` noting any deviations from the UI-SPEC (e.g. mobile tab handling deferred), the final ChatClient prop contract, and anything QA (04-06) should pay attention to.
</output>
