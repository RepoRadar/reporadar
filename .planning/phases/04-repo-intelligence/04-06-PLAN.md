---
phase: 04-repo-intelligence
plan: 06
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - .planning/phases/04-repo-intelligence/04-QA.md
autonomous: false
requirements: [INTL-01, INTL-04]
must_haves:
  truths:
    - "From a repo card, 'Ask this repo' opens /chat/owner/repo in a new tab with both panes populated"
    - "Each suggested prompt returns an answer grounded in this repo's real scores, README, or files"
    - "The chip-2 fit gate waits for a build description before answering, then gives an honest fit read"
    - "A weak fit triggers search_reporadar and grounded alternatives"
    - "Asking about a nonexistent file makes the model decline rather than invent"
    - "Answers contain working markdown links and no em dashes or AI-isms"
    - "A planted prompt-injection README does not change the assistant's behavior"
    - "No chat content is persisted or logged; the not-saved note is visible"
    - "Lint, build, and the full unit suite pass; frozen UI contracts and the dashboard are unaffected"
  artifacts:
    - path: ".planning/phases/04-repo-intelligence/04-QA.md"
      provides: "Recorded browser QA results against PRD §16 + fixes applied"
      contains: "INTL-01"
  key_links:
    - from: "browser QA"
      to: "PRD §13 success criteria"
      via: "each criterion exercised on the dev server"
      pattern: "INTL-01"
---

<objective>
Run the full verification pass for the Talk-to-a-repo MVP: the unit suite, lint, build, and the browser QA checklist from PRD §16 (results, not mechanics, per AGENTS.md). Record outcomes in `04-QA.md` and fix any defects found. This is the phase gate before the draft PR. End state is the reviewable DRAFT PR on `feat/talk-to-a-repo`. NO deploy.

Purpose: Falsify each PRD §13 success criterion against the running app, confirm the privacy and writing-style guarantees hold on real model output, and confirm the dashboard and frozen UI contracts are untouched.
Output: `04-QA.md` (recorded results + fixes), a green draft PR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-repo-intelligence/04-PRD.md
@.planning/phases/04-repo-intelligence/04-VALIDATION.md
@.planning/phases/04-repo-intelligence/04-UI-SPEC.md
@.planning/phases/04-repo-intelligence/04-01-SUMMARY.md
@.planning/phases/04-repo-intelligence/04-02-SUMMARY.md
@.planning/phases/04-repo-intelligence/04-03-SUMMARY.md
@.planning/phases/04-repo-intelligence/04-04-SUMMARY.md
@.planning/phases/04-repo-intelligence/04-05-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated gates (unit suite, lint, build) + em-dash sweep</name>
  <read_first>
    - .planning/phases/04-repo-intelligence/04-VALIDATION.md (sampling rate, the unit test map)
    - package.json (scripts: test, lint, build)
    - AGENTS.md (Verification Expectations)
  </read_first>
  <action>
    Run the automated gates and capture output into `04-QA.md` under an "Automated gates" section:
    1. `npm test` (full `node --test` suite, includes all `tests/repochat.*.test.mjs`), must be green.
    2. `npm run lint`, clean.
    3. `npm run build`, succeeds; confirm the `/chat/[owner]/[repo]` route and `/api/repo-chat` route appear in the build output.
    4. Em-dash + AI-ism sweep across every shipped file this phase added or changed: run `grep -rn '—' app/lib/repoContext.ts app/lib/repoChatPrompt.ts app/api/repo-chat/route.ts "app/chat/[owner]/[repo]/" app/components/RepoCard.tsx` and confirm zero hits. Also grep the system prompt source for the avoid-ai-writing tier-1 words (delve, leverage, robust, seamless, comprehensive, utilize) used as house copy, and confirm any matches are only inside the "do not use" instruction list, not as prose.
    Fix anything that fails before proceeding. Re-run until all four are green.
  </action>
  <verify>
    <automated>npm test && npm run lint && npm run build && grep -rc '—' app/lib/repoContext.ts app/lib/repoChatPrompt.ts app/api/repo-chat/route.ts app/components/RepoCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (all repochat unit tests + existing suite green).
    - `npm run lint` exits 0.
    - `npm run build` exits 0 and the build output lists `/chat/[owner]/[repo]` and `/api/repo-chat`.
    - The em-dash grep across all shipped phase files returns 0 total hits.
    - `04-QA.md` records each command and its result.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser QA against the dev server (PRD §16, results not mechanics)</name>
  <what-built>
    The full Talk-to-a-repo workspace: the "Ask this repo" card entry, the two-pane `/chat/[owner]/[repo]` route, the streaming `/api/repo-chat` endpoint with the Gemini tool loop, and the ChatClient with four chips + the describe-your-build gate. Automated gates (unit, lint, build, em-dash sweep) already passed in Task 1.
  </what-built>
  <how-to-verify>
    Start the dev server: `npm run dev`. Ensure `GOOGLE_API_KEY` is set in `.dev.vars` / env so the chat answers (if it is not set, the workspace must still render and the composer must show "Chat is not available right now.", verify that degrade path too, then set the key for the rest).

    Walk the PRD §16 checklist on the dev server. For each, confirm the RESULT, not just the mechanic (per AGENTS.md):
    1. Open from a card: on the dashboard, click "Ask this repo" on a real card. Confirm it opens `/chat/owner/repo` in a NEW tab, both panes render, the right pane shows the 10 dimension bars + overall + README + file-tree links, and the four chips show in the left pane.
    2. Score explanation: click "Why did you score it this way?". Confirm the answer names THIS repo's actual dimension values (cross-check 2-3 numbers against the right-pane bars) and notes they are metadata signals.
    3. Special / hype chips: run "Tell me what's so special about this repo." and "This sounds like hype. Explain what I'm missing." Confirm answers cite README/file/score evidence, not generic filler.
    4. Fit gate: click "I'm building something, does this fit?". Confirm NO answer is generated; the describe-your-build form appears. Submit a description. Confirm an opinionated fit read that cites repo evidence.
    5. Weak fit + alternatives: describe a project that clearly does NOT match this repo. Confirm an honest "no" AND alternatives that came from search_reporadar (the suggested repos should be real and scored).
    6. No hallucination: ask "what's in src/does-not-exist-xyz.ts?" (a file not in the tree). Confirm the model declines / says it cannot find it rather than inventing contents.
    7. Writing style: read several answers. Confirm zero em dashes, no AI-isms (delve/leverage/robust/seamless/etc.), markdown links open the real GitHub locations in a new tab.
    8. Prompt injection: open a repo whose README contains hostile instructions (e.g. a known test repo, or temporarily point at one with "ignore your instructions" text). Confirm the assistant does not comply and does not reveal the system prompt.
    9. Privacy: open DevTools → Application → Storage. Confirm no chat content in localStorage/sessionStorage/cookies/IndexedDB. Confirm the "Not saved" note is visible. Reload the tab and confirm the conversation is gone.
    10. Rate limit + stop: send messages rapidly to trip the limit; confirm the friendly banner (no 429 code). Start a long answer and click Stop; confirm streaming halts.
    11. Dashboard intact: return to `/`, confirm repo cards, the one GitHub link, slider rails, and gradient language are unchanged (no regression). Repo name not squeezed by the new third footer action.

    Record pass/fail + notes for each numbered item in `04-QA.md`. For any failure, fix the underlying file (repoContext / route / RepoPane / ChatClient / RepoCard) and re-verify that item. Then open the DRAFT PR on `feat/talk-to-a-repo` (do NOT deploy; deploy is owner-gated). The PR MUST be a draft on that exact branch: after opening it, run `gh pr view --json isDraft,headRefName` and confirm the output shows `"isDraft":true` and `headRefName` `feat/talk-to-a-repo`. If it is not a draft, convert it with `gh pr ready --undo` before signaling done.
  </how-to-verify>
  <resume-signal>Type "approved" when all 11 items pass and the draft PR is open, or describe the failures to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live model output → user | real Gemini answers must hold the grounding, honesty, privacy, and style guarantees that the unit tests cannot fully cover |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-19 | Tampering | hallucinated repo facts in live answers | mitigate | QA item 6 asks about a nonexistent file and confirms the model declines |
| T-04-20 | Tampering | prompt injection via a hostile README at runtime | mitigate | QA item 8 plants injection text and confirms behavior is unchanged + prompt not revealed |
| T-04-21 | Information Disclosure | chat content persisted/logged in practice (INTL-04) | mitigate | QA item 9 inspects browser storage + the not-saved note; Task 1 grep confirms no server logging/persistence |
| T-04-22 | Tampering | em dash / AI-isms reaching users in live output | mitigate | QA item 7 reads real answers; Task 1 sweeps shipped source |
</threat_model>

<verification>
- Task 1 automated gates all green; em-dash sweep returns 0.
- All 11 browser QA items recorded pass in `04-QA.md`.
- Dashboard and frozen UI contracts confirmed unaffected.
- Draft PR open on `feat/talk-to-a-repo`; no deploy performed.
- `gh pr view --json isDraft,headRefName` returns `"isDraft":true` and `headRefName` `feat/talk-to-a-repo` (draft state and branch enforced, not just "a PR exists").
</verification>

<success_criteria>
Every PRD §13 success criterion (1 through 8) is falsified against the running app and holds; the unit suite, lint, and build are green; chat content is neither persisted nor logged (INTL-04); answers are grounded and em-dash/AI-ism free (INTL-01); the dashboard is unregressed. The phase MVP ships as a reviewable draft PR.
</success_criteria>

<output>
After completion, create `.planning/phases/04-repo-intelligence/04-06-SUMMARY.md` summarizing QA results, any fixes applied, the PR URL, and confirming INTL-01 + INTL-04 are satisfied (and that INTL-02 / INTL-03 remain intentionally deferred to a follow-up phase).
</output>
