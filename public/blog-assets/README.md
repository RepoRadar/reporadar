# Blog assets

Static images referenced by posts in `app/content/blog/`.

Expected files:

- `team-building-reporadar.jpg` — photo of Christo and Craig building RepoRadar in
  San Francisco (Priyanshu remote). Used at the top of `meet-the-founders`. Until
  this file is added, that post shows a broken-image placeholder.
